# PEBKAC Vault Configuration
# Real credentials live here — the model never sees this file.
# The harness generates scoped proxy credentials instead.
#
# Add your secrets below:
# secrets:
#   aws_production:
#     real_key: "AKIAIOSFODNN7EXAMPLE"
#     proxy_generator: "aws_sts_session"
#     scope:
#       - "s3:ReadOnly"
#     expiry: "1h"

secrets: {}

# Test Secret (added during vault walkthrough)
secrets:
  - name: TEST_API_KEY
    value: test-secret-1775189088297-n7da90
    scope: [test]
